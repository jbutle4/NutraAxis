{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "sync-staging": "node -e \"require('./src/lib/staging-sync').runStagingDatabaseSync().then((summary) => { console.log(JSON.stringify(summary, null, 2)); }).catch((err) => { console.error(err.message); process.exit(1); })\"",
    "backfill-process-retries": "node scripts/backfill-process-retries.js",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/service-bus": "^7.9.5",
    "mssql": "^11.0.1",
    "nodemailer": "^6.10.1"
  }
}
