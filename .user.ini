; Production: avoid re-statting every PHP file on each request (slow on Azure storage).
; Restart the App Service after deploys, or set ASSET_VERSION in app settings when CSS changes.
opcache.validate_timestamps=1
opcache.revalidate_freq=300
